import { Editor } from "@/components/DynamicEditor.tsx";

const Dashboard = () => {
  return (
    <div style={{ height: "100vh" }}>
      <Editor></Editor>
    </div>
  );
};
export default Dashboard;
